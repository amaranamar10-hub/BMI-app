import 'package:flutter/material.dart';
void main() => runApp(MyApp());
class MyApp extends StatelessWidget {
  @override Widget build(BuildContext context) {
    return MaterialApp(title: 'BMI', home: BmiCalculator(), debugShowCheckedModeBanner: false);
  }
}
class BmiCalculator extends StatefulWidget {
  @override State createState() => _BmiCalculatorState();
}
class _BmiCalculatorState extends State<BmiCalculator> {
  double h=170; int w=70; String r="";
  void calc(){ setState((){ r="Your BMI: ${(w/((h/100)*(h/100))).toStringAsFixed(1)}"; }); }
  @override Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("BMI Calculator")),
    body: Padding(padding: EdgeInsets.all(20),
    child: Column(children:[
      Text("Height: ${h.toInt()} cm", style:TextStyle(fontSize:18)), 
      Slider(value:h, min:120, max:220, onChanged:(v){setState((){h=v;});}),
      Text("Weight: $w kg", style:TextStyle(fontSize:18)), 
      Slider(value:w.toDouble(), min:30, max:150, onChanged:(v){setState((){w=v.toInt();});}),
      SizedBox(height:20),
      ElevatedButton(onPressed:calc, child:Text("Calculate BMI")), 
      SizedBox(height:20),
      Text(r, style:TextStyle(fontSize:24, fontWeight: FontWeight.bold))
    ])));
  }
}
name: bmi_app
description: A simple BMI Calculator
version: 1.0.0
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
flutter:
  uses-material-design: true